const assert = require('assert'); const { auditSpend, fallbackSummary } = require('../lib/audit-engine.runtime.cjs');
const cases=[]; function add(name,input,check){cases.push({name,input,check});}
for(let i=0;i<10;i++) add(`BEST ${i+1}: high value stack`,{teamSize:4,primaryUseCase:'coding',tools:[{tool:'Cursor',plan:'Business',monthlySpend:160,seats:4,useCase:'coding'},{tool:'GitHub Copilot',plan:'Business',monthlySpend:76,seats:4,useCase:'coding'},{tool:'OpenAI API',plan:'API direct',monthlySpend:800+i*50,seats:1,useCase:'coding'}]},r=>{assert(r.totalMonthlySavings>300);assert(r.confidenceScore>=80);assert(r.recommendations.some(x=>x.category==='api_optimization'));});
for(let i=0;i<10;i++) add(`GOOD ${i+1}: rightsize small team`,{teamSize:2,primaryUseCase:'mixed',tools:[{tool:'ChatGPT',plan:'Team',monthlySpend:60,seats:2,useCase:'writing'},{tool:'Claude',plan:'Pro',monthlySpend:40,seats:2,useCase:'research'}]},r=>{assert(r.totalMonthlySavings>=20);assert(['low','medium'].includes(r.tier));assert(fallbackSummary(r).length>50);});
for(let i=0;i<10;i++) add(`WORST ${i+1}: malformed or optimal`,{teamSize:i-5,primaryUseCase:'mixed',tools:i%2?[]:[{tool:'Gemini',plan:'Pro',monthlySpend:-20,seats:0,useCase:'research'}]},r=>{assert(r.totalMonthlySavings>=0);assert(r.totalAnnualSavings>=0);assert(Number.isFinite(r.confidenceScore));});
let pass=0; for(const c of cases){const r=auditSpend(c.input); c.check(r); pass++;} console.log(`Passed ${pass}/${cases.length} production audit-engine tests`);
